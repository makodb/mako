[package]
name = "mako-cmake-bridge"
version = "0.1.0"
edition = "2021"
publish = false
build = "build.rs"

[dependencies]

[build-dependencies]
fragile-driver = { git = "https://github.com/shuaimu/fragile.git", branch = "main", package = "fragile-driver" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
shell-words = "1"
