# Cargo Native Specs

`targets.toml` stores Cargo-native C/C++ target metadata used by
`cargo-mako-native-build`.

- It is generated by `cargo mako-native export-spec ...`.
- Builds from this spec do not invoke CMake.
- `${REPO_ROOT}` placeholders are expanded at build time.
