# Redis TCL Known Skips

# Exact stream-only tests skipped because the current Redis compatibility
# plan does not include Redis Streams commands.

Blocking commands ignores the timeout
COPY basic usage for stream
COPY basic usage for stream-cgroups

# Redis keyspace notification pubsub is not part of the current compatibility surface.
/^Keyspace notifications:
SMOVE only notify dstset when the addition is successful
