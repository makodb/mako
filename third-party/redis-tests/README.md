# Redis TCL Tests

Vendored from `redis/redis` tag `7.4.0`, commit
`c9d29f6a918c335bc1778d9f68e521c1bbb36a0f`, using
`tools/redis_compat/bootstrap_redis_tests.sh`.

Only the upstream `tests/` tree is copied. `run_tcl_suite.sh` runs the
external-server subset against `makoCon`.
