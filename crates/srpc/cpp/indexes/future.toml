version = 1

[modules."rrr::reactor"]
cpp_module = "rrr.reactor"
namespace = "rrr"

[modules."rrr::reactor".symbols.BoxEvent]
kind = "type"
callable_signatures = []

[modules."rrr::reactor".symbols.create_sp_box_event]
kind = "function_template"
callable_signatures = ["rusty::Arc<BoxEvent<T>>()"]

[modules."rrr::reactor".symbols."BoxEvent::is_ready"]
kind = "method"
callable_signatures = ["bool()"]

[modules."rrr::reactor".symbols."BoxEvent::set"]
kind = "method"
callable_signatures = ["void(const T&)"]

[modules."rrr::reactor".symbols."BoxEvent::wait"]
kind = "method"
callable_signatures = ["void()"]

[modules."rrr::reactor".symbols."BoxEvent::wait_timeout"]
kind = "method"
callable_signatures = ["void(uint64_t)"]

[modules."rrr::reactor".symbols."BoxEvent::get"]
kind = "method"
callable_signatures = ["T()"]

[modules."std"]
cpp_module = "std"
namespace = "std"

[modules."std".symbols.make_pair]
kind = "function_template"
callable_signatures = ["std::pair<A,B>(A,B)"]
