[package]
name = "mrx"
version = "0.1.0"
edition = "2021"
rust-version = "1.91"
description = "Masstree write-back cache over RocksDB"
license = "MIT"
publish = false

[dependencies]
mrx-core = { path = "../mrx-core" }
mrx-masstree = { path = "../mrx-masstree" }
mrx-rocks = { path = "../mrx-rocks" }
# See crates/Cargo.toml: profiles live at the workspace root.
