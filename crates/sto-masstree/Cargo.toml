[package]
name = "sto-masstree"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
build = "build.rs"

[lib]
path = "src/lib.rs"

[features]
fixed-u64 = []

[dependencies]
arc-swap = "1.7"
masstree = { path = "../masstree" }
sto-core = { path = "../sto-core" }

[dev-dependencies]
mako-history = { path = "../mako-history" }
sto-test-datatypes = { path = "../sto-test-datatypes" }

[lints.rust]
unsafe_code = "deny"
