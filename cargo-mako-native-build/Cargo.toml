[package]
name = "mako-native-build"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
fragile-driver = { git = "https://github.com/shuaimu/fragile.git", branch = "main", package = "fragile-driver" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
shell-words = "1"
toml = "0.8"
walkdir = "2"
